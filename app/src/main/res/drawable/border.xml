<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color set to white -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- Border color -->
    <corners android:radius="8dp" /> <!-- Rounded corners (optional) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Inner padding -->
</shape>
