<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        tools:targetApi="31">
        <activity
            android:name=".RestaurantDetailsActivity"
            android:exported="false" />
        <!-- MainActivity (Splash Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- HomeActivity (Main Page after Splash) -->
        <activity
            android:name=".HomeActivity"
            android:exported="true" /> <!-- About Page -->
        <activity
            android:name=".AboutActivity"
            android:exported="false" /> <!-- Add Restaurant Page -->
        <activity
            android:name=".AddRestaurantActivity"
            android:exported="false" /> <!-- Restaurant List Page -->
        <activity
            android:name=".RestaurantListActivity"
            android:exported="false" />
    </application>

</manifest>